import Navigation from "../navigation/Navigation";
import Hero from "../hero/Hero";

const Header = () => {
  return (
    <div id="home">
      <Navigation />
      <Hero />
    </div>
  );
};

export default Header;
